package minipar.lexer;

public enum  TokenType {
    KEYWORD,
    IDENTIFIER,
    NUMBER,
    STRING,
    OPERATOR,
    DELIMITER,
    COMMENT,
    WHITESPACE,
    NEWLINE,
    INDENT,
    DEDENT,
    EOF
}
